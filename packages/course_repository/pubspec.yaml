name: course_repository
description: The Repo that handles courses
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  random_x: ^0.3.1
    
  equatable: ^2.0.5
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.9.2
  rxdart: ^0.27.7
  firebase_storage: ^11.6.0
  # rxdart: ^0.27.7
  # path: ^1.8.3

  user_repository:
    path: ../user_repository
  location: ^5.0.3
  google_maps_flutter: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true